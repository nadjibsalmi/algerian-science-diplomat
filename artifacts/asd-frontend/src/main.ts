/**
 * main.ts — Application entry point
 *
 * Creates the Vue app and mounts it to #app.
 * All plugin/component registration is in app.ts.
 */
import { createVueApp } from './app'

createVueApp().mount('#app')
